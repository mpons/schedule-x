import { datePickerItIT } from './date-picker'
import { Language } from '../../types/language.translations'
import { calendarItIT } from './calendar'
import { eventModalItIT } from './event-modal'

export const itIT: Language = {
  ...datePickerItIT,
  ...calendarItIT,
  ...eventModalItIT,
}
